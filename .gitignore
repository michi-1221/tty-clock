# Binaries
/tty-clock
/dist/

# Go test / coverage artifacts
*.out
*.test

# Secrets / credentials — NEVER commit (real tokens live in GitHub Secrets)
.npmrc              # may hold //registry.npmjs.org/:_authToken=...
.env                # local goreleaser/npm tokens (GITHUB_TOKEN, NPM_TOKEN)
.env.*
!.env.example
*.pem
*.key
*.p12
.goreleaser-token

# Local npm test installs
node_modules/
/package.json
/package-lock.json

# Editor / OS noise
.DS_Store

# Local Claude guidance — kept on disk, out of the repo
/CLAUDE.md
